import {ArrowLeft,CalendarCheck,Fuel,Gauge} from "lucide-react";import {Link,useParams} from "react-router-dom";import {cars} from "../data/cars";
export default function CarDetails(){const{id}=useParams();const car=cars.find(c=>c.id===id);if(!car)return <main className="section empty-page"><h1>Car not found</h1><Link to="/cars" className="btn primary">Back to Cars</Link></main>;return <main className="section page"><Link to="/cars" className="back-link"><ArrowLeft size={17}/> Back to collection</Link><div className="detail-card"><img src={car.image} alt={car.name}/><div className="detail-info"><div className="eyebrow dark">{car.type.toUpperCase()}</div><h1>{car.name}</h1><div className="detail-price">${car.price.toLocaleString()}</div><p>{car.description}</p><div className="detail-meta"><span><Gauge/> {car.speed}</span><span><Fuel/> {car.fuel}</span></div><Link to="/booking" className="btn primary"><CalendarCheck size={18}/> Book this car</Link></div></div></main>}