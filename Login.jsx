import {Car,LockKeyhole,Mail} from "lucide-react";import {useState} from "react";import {useLocation,useNavigate} from "react-router-dom";
export default function Login(){const nav=useNavigate();const loc=useLocation();const[f,setF]=useState({email:"",password:""});function submit(e){e.preventDefault();if(!f.email||!f.password)return alert("Please fill all fields.");localStorage.setItem("carhub_logged_in","true");nav(loc.state?.from||"/dashboard")}return <main className="auth-page"><form className="auth-card" onSubmit={submit}><div className="auth-logo"><Car size={24}/></div><div className="eyebrow dark">WELCOME BACK</div><h1>Login to CarHub</h1><p>Enter any email and password for this frontend demo.</p><label>Email</label><div className="input-icon"><Mail size={18}/><input type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})} placeholder="you@example.com"/></div><label>Password</label><div className="input-icon"><LockKeyhole size={18}/><input type="password" value={f.password} onChange={e=>setF({...f,password:e.target.value})} placeholder="Password"/></div><button className="btn primary full">Login</button></form></main>}