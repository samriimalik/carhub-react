import {ArrowRight,Fuel,Gauge} from "lucide-react";import {Link} from "react-router-dom";
export default function CarCard({car}){return <article className="car-card"><div className="car-image-wrap"><img src={car.image} alt={car.name}/><span className="car-tag">{car.type}</span></div><div className="car-card-body"><h3>{car.name}</h3><p className="card-price">${car.price.toLocaleString()}</p><div className="car-meta"><span><Gauge size={16}/> {car.speed}</span><span><Fuel size={16}/> {car.fuel}</span></div><Link className="details-link" to={`/cars/${car.id}`}>View Details <ArrowRight size={17}/></Link></div></article>}